#include "main.h"

/**
 * print_rev - a function that prints a string, in reverse.
 * @s: The parameter.
 * Return: Nothing.
 */

void print_rev(char *s)
{
	while (*s != '\0')
		int count++;
	while (count)
		_putchar(s[--count]);
	_putchar('\n');
}
